/**
 * Feature manifest — importing a feature module registers it in the Feature
 * Registry. Adding a feature = create its folder and add one import line here.
 */
import "./wallpaper";
import "./avatar";
import "./clock-weather";
import "./search-bar";
import "./most-visited";
import "./daily-quote";
import "./bookmark-bar";
// Phase 2 — left sidebar panels
import "./panel-weather-detail";
import "./panel-news";
// Phase 3 — OAuth integrations (order: GitHub → Spotify → Calendar per docs)
import "./panel-github";
import "./panel-spotify";
import "./panel-calendar";
// Phase 4 — right sidebar tools (window manager)
import "./tool-pomodoro";
import "./tool-tasks";
import "./tool-notes";
import "./tool-qr";
import "./tool-emoji";
