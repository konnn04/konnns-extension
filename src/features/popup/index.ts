/**
 * Registers every popup-native widget (side-effect imports populate the
 * Popup Widget Registry) — docs/roadmap/00-tong-quan.md §2.3. Adding a
 * widget = new folder + one line here, same pattern as `features/site`.
 */
import "./audio-mixer";
